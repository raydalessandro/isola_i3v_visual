# dependencies
node_modules/
.pnp
.pnp.js

# testing
coverage/

# next.js
.next/
out/

# production
build/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env
.env*.local
.env

# vercel
.vercel

# build-time copia dati dal catalogo: TRACCIATI dal 2026-06-10 (cutover Vercel).
# I .json di public/data/ sono il "mirror dispatch" per il deploy Next.js
# quando Vercel ha Root Directory = web/ (non puo' leggere ../catalogo_web/).
# Aggiornarli con: `npm run copy-data` (dal vivo locale) o lo script Python
# scripts/build_catalogo_web.py (genera entrambe le copie).
# public/data/  --  ora tracciato, niente regola .gitignore qui

# typescript
*.tsbuildinfo
next-env.d.ts

# package-lock.json: tracciato dal 2026-06-10 (blindatura supply-chain).
# 452 pacchetti pinnati per build riproducibili — vedi
# docs/BLINDATURA_2026-06-09.md.
